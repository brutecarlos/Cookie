Title: Cookie — Daily Fortune

Short description:
Shows one small fortune-cookie style quote per day. The daily sequence is fixed at install and cannot be changed without reinstalling.

Full description:
Cookie presents one gentle, uplifting fortune-cookie style quote each day. Quotes are bundled with the extension and the extension stores an immutable install seed so that the daily sequence is deterministic and cannot be edited by the user. This keeps the experience consistent and tamper-proof across normal use — to reset the sequence the user must remove and reinstall the extension.

Features:
- One quote per day — unobtrusive and calming.
- Deterministic sequence fixed at install (no in-app editing).
- Quick copy & share controls for each quote.
- Lightweight, privacy-friendly — all data stays local.
- Modern, subtle UI that auto-dismisses after 10s (or can be dismissed manually).

Privacy / data usage summary (short):
No personal data is collected or transmitted. The extension only stores an `installTime` and a local `seed` in `chrome.storage.local` to determine daily rotation. Quotes are bundled and served locally.

Support / contact:
Support email: carlosluquetrad@gmail.com

Promotional text:
Receive one small daily thought — a tiny, predictable moment of calm in your day. Cookie keeps it simple, private and reliable.

What to upload to the Web Store:
- `cookie-extension.zip` (generated by `package.sh`)
- Icons (16/48/128) — already present in `icons/`
- 2–5 screenshots (replace the placeholders in `store/screenshots/` with real PNGs sized per Web Store requirements)
- Privacy policy URL or text (see `store/privacy_policy.md`)
